import * as React from 'react';
import { RootProps, RootRef, TextProps } from './index.js';
import { TextRef } from '@rn-primitives/types';
import './label';
import 'react-native';

type RootComponentProps = RootProps & React.RefAttributes<RootRef>;
declare const Root: {
    ({ asChild, ref, ...props }: RootComponentProps): React.JSX.Element;
    displayName: string;
};
type TextComponentProps = TextProps & React.RefAttributes<TextRef>;
declare const Text: {
    ({ asChild, ref, ...props }: TextComponentProps): React.JSX.Element;
    displayName: string;
};

export { Root, Text };
